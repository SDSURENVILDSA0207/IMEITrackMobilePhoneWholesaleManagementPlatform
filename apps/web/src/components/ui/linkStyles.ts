/** Default inline app link — soft underline on hover + 150ms color transition. */
export const textLinkClass = "text-link";

/** Muted (slate) inline link for secondary navigation. */
export const textLinkMutedClass = "text-link-muted";

/** Table row primary keys: slate → brand on hover with subtle underline. */
export const textLinkNeutralClass = "text-link-neutral";

/** Compact “Edit” / chip-style links in tables (background on hover, no underline). */
export const textLinkChipClass = "text-link-chip";

/** Rounded pill “View all” style in section headers. */
export const textLinkPillClass = "text-link-pill";
